# Moment Catcher - React Frontend

React 前端应用，连接 FastAPI 后端。

## 🚀 快速开始

### 1. 安装依赖

```bash
cd frontend
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

前端将在 `http://localhost:3000` 启动

### 3. 确保后端运行

后端需要在 `http://localhost:8000` 运行：

```bash
# 在项目根目录
python run_api.py
```

## 📁 项目结构

```
frontend/
├── src/
│   ├── components/
│   │   ├── LandingPage.jsx      # 登录页面
│   │   ├── MainInterface.jsx    # 主界面（Tab导航）
│   │   ├── ChatInterface.jsx    # 对话界面
│   │   ├── MemoriesView.jsx     # Memories 列表
│   │   └── SettingsView.jsx     # 设置页面
│   ├── services/
│   │   └── api.js               # API 调用封装
│   ├── App.jsx                  # 主应用组件
│   ├── main.jsx                 # 入口文件
│   └── index.css                # 全局样式
├── index.html
├── vite.config.js
└── package.json
```

## 🔧 开发计划

### ✅ Phase 1: 基础功能（当前）
- [x] Landing Page
- [x] 基础对话界面（文字输入）
- [x] 消息显示
- [x] Moment 管理
- [x] Memories 列表
- [x] 设置页面

### ⏳ Phase 2: 语音识别
- [ ] 麦克风录音 UI
- [ ] 音频上传到后端
- [ ] ASR 结果显示
- [ ] 识别文字自动填入输入框

### ⏳ Phase 3: 视觉效果
- [ ] 粒子背景
- [ ] Moment Card 视觉优化
- [ ] 3D Memory Nebula
- [ ] 动画效果

## 📝 API 端点

所有 API 调用都在 `src/services/api.js` 中封装：

- `initAPI()` - 初始化连接
- `startMomentAPI()` - 开始新 Moment
- `chatAPI()` - 发送消息
- `saveMomentAPI()` - 保存 Moment
- `getMomentsAPI()` - 获取所有 Moments
- `getStyleProfileAPI()` - 获取风格画像
- `ttsAPI()` - 文本转语音
- `asrAPI()` - 语音转文字（新增）

## 🎨 样式

使用 CSS Modules，每个组件有独立的 CSS 文件。

主题色：
- 主色：`#667eea`（紫色渐变）
- 背景：深色渐变 `#0a0a0a` → `#1a1a2e`
- 文字：`#e0e0e0`

## 🔄 下一步

1. ✅ 基础文字输入功能（已完成）
2. ⏳ 集成语音识别
3. ⏳ 添加视觉效果


